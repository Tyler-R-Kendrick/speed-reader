// Import the built webcomponents bundle (assume dist or direct import)
// You may need to adjust the import path depending on your build setup
import '../../webcomponents/src/components/rsvp-player';

// Optionally, you can set the text property programmatically here
// const player = document.querySelector('rsvp-player');
// if (player) player.text = 'Hello from the webapp!';
